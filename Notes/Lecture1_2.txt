CSE 446/598
Software Integration and Engineering

Unit 1
Introduction and Advanced Service Development

Lecture 1-2
Raw Services and Self-Hosting
Dr. Yinong Chen
https://myasucourses.asu.edu/

Lecture Overview

2










WCF Basic Service Development and Consuming (Review)
Service is an active API with language dependent or
independent interface;
API can be active (hosted) or passive, and it can have
language dependent or independent interface.
API example: Raw Service Development and Self-Hosting

Raw Service development: Develop the interface without
using the service development tools
Host (worker process) development: Make it active


Endpoint creation
Proxy creation

Different Clients Consuming Self-Hosting Services
Y. Chen

Model of Service-Oriented
Software Development

3

Three-Party Development Model
The Rest
Units

Service brokers
Registry

This
Unit

Application builders

Service providers

Applications

Active objects
Service hosting

Service consumers

Y. Chen

Service/Application Development Environments

4

ASP.NET and Windows Communication
Foundation (WCF) (Text Chapter 3)
 Service development in Python/Flask
 Workflow Foundation (Text Chapter 7) and
Power Platform (New)
 Oracle SOA Suite/ JDeveloper (Text Chapter 8)
 AI-assisted IDEs: Power Platform and Antigravity


Java-based SOA Development: Netbeans, Eclipse/Axis/Tomcat
 JAX-RPC 1.1 API
 J2EE
 BEA WebLogic
 IBM WebSphere
 SAP Enterprise Service Architecture (ESA)
Y. Chen


WCF Service Development and Consuming

5


ASP .Net framework is a component in Visual Studio for
service and application development.





It can be used for service development (.asmx service)
Different at
It is mainly used for application development

WCF is a component in Visual Studio




language
interface level

WCF is used for service development only (.svc service)
It is used for developing SOAP as well as RESTful services
WCF service hosting: with or without IIS

Same at
service
interface
level

Service Interfaces: Both .asmx and .svc services use WSDL
 WCF service consuming







Console application
ASP .Net Windows forms application (Desktop application)
ASP .Net Website (Web application) / HTML5 Website
BPEL, and many others
Y. Chen

6

WCF Interface between Client and Service
WCF Service.svc
WCF Client

Endpoint
A

Proxy
C

B

B

C

Endpoint
A

A

B

C

Endpoint
A

B

C

Address
(where)
Proxy to
Endpoint

Binding
(how)
Contract
(what)

Please read text
section 3.1 for more
detail
Y. Chen

7

Endpoint Coded in WSDL
An ASP .Net endpoint is the port to
the method exposed through a
markup of contract in program
 WCF endpoint contains ABC:


definitions
service
WSDL
endpoint

port
(URL)
binding
(soap)

Address
(where)

portType

Binding
(how)

operation

Contract
(what)
message, in
type
message, out
type



WCF
endpoint

Different development tools (.asmx,
.svc, and others) have different
structures, but contain the same basic
information
 All are coded in WSDL
Y. Chen

Developing WCF Basic Services & Clients

8

Developing a Service using WCF tools (Review)
 Developing a WCF service using template
 Hosting the service using IIS Express Server
 Developing a console application to consume the
WCF service
 Developing WCF Service with Self-Hosting


Y. Chen

9

Develop Web Services Using WCF

Text Chapter 3,
section 3.2.1
We did this
exercise in
CSE445
Assignment 1

Y. Chen

Step 2: Define Service Contract / Interface

10

1

// IService.cs file
using System.ServiceModel;

interface

[ServiceContract]
public interface IService {
[OperationContract]
string GetData(int value); // generated code, rename it
//[OperationContract]
TODO: Add your service operation contracts here
double PiValue();
[OperationContract]
int absValue(int intVal);

// my operation 1
// my operation 2

}
Y. Chen

11

Step 3: Implement Service Contract (behavior)
2
// Service.svc.cs file
using System.ServiceModel;

public class Service : IService {
public string GetData(int value) {
return string.Format("You entered: {0}", value);
}
public double PiValue() {
double pi = System.Math.PI;
return (pi);
}
public int absValue(int x) {
if (x >= 0) return (x);
else return (-x);
}
}

interface
Service
implementation
(behavior)

Y. Chen

12

Step 4: Build and View in Browser
Right-click file Service.svc and choose “View in Browser”

The service is hosted
on localhost / IIS
Express

Click on the link:
http://localhost:57956/BasicThreeSvc/Service.svc?wsdl

To view the WSDL file

Y. Chen

13

Built-in Test Client
Select the service
Menu: DEBUG  Start Without Debugging
The Test Client Window will be open:

Read text for
other ways of
starting the Test
Client: From
CMD

Y. Chen

14

Hosting Using Localhost / IIS Express
Interoperable
Services
WSDL-Based
Standard Interface
interface
Service
implementation
(behavior)

Hosting Service in
Localhost / IIS Express

Y. Chen

Step 5: Create a Client to Test the Service

15

Add a Console Application into the same solution
 Add Service Reference:


http://localhost:28957/BaisicThree/Service.svc?wsdl

Rename to
myServiceRef

Please read text
section 3.6.4 for
more detail

Y. Chen

16

Step 6: Write Client Code to Invoke Service
static void Main(string[] args) { // console application
myServiceRef.ServiceClient myProxy = new myServiceRef.ServiceClient();
double pi = myProxy.PiValue(); // call PiValue operation
Int32 test1 = 27; // Create test input 1
Int32 test2 = -132; // Create test input 2
Int32 result1 = myProxy.absValue(test1); // call operation
Int32 result2 = myProxy.absValue(test2); // call operation
Console.WriteLine("PI value = {0}", pi);
Console.WriteLine("Absolute values of {0} is {1} and of {2} is {3}", test1,
result1, test2, result2);
Console.WriteLine("GetData = {0}", myProxy.GetData(1234));
// Close the proxy, the channel to the service
myProxy.Close();
}
Y. Chen

Step 7: Test the WCF Client and Service

17

First, start the service by right-click the Service.svc
and choose “View in Browser”  Keep service
running
 Right-click the client project and choose:
Set as StartUp Project
 Start without Debugging


Y. Chen

18

Different Ways of Hosting WCF Services

Internet

.Net
Localhost /
IIS Express

CSE445

IIS
Server

SelfHosting

Cloud
Server

Reduced
overhead;
Increased
flexibility

Increased
scalability in
software &
hardware
Y. Chen

Developing Self-Hosting API and Clients

19

WCF Basic Service Development (Review)
 The process is largely automated
 Developing an API with Self-Hosting step by step
 Developing API without using service template tools;
 Developing the hosting service;
 Creating endpoint and proxy
 Consuming the API in a command line client;
 Consuming the API in ASP .Net website


Y. Chen

Reasons of Using Self-Hosting

20

SelfHosting

Reduced overhead for efficiency
 IIS (Internet Information Services) and
WAS (Windows Process Activation Services)
are designed to deal with all possible scenarios of service
hosting (https://docs.microsoft.com/en-us/dotnet/framework/wcf/feature-details/hosting-in-windows-process-activation-service)
 Service interface and service protocol can increase
overhead
 Self-hosting can avoid many overheads
 Increased flexibility/controllability
 IIS and WAS decide when a service starts and terminates.
 With self-hosting, you can decide when to start and when to
close a service.


Y. Chen

Case Study of Using Self-Hosting Service for
IoT and Robotics Apps
A Motivation for Self-Hosting
Web Simulation Environment

Visual IoT/Robotics
Programming Language
Environment

Robot as a Service at ASU,
with self-hosting service
Y. Chen

Self-Hosting Using Multithreading

22

Write your own Worker process, which can run in
an infinite loop, or until a condition is met;
 Write your service methods
 Worker process (Main) activates each service
method as a thread – active object (design pattern)
 Without implementing service management, one
client can call the service at a time;
 To implement multiple accesses at the same time,
implement service queue, as discussed in Text
Chapter 2.


Y. Chen

Self-Hosting Project Overview

23

Service Solution: SelfhostingService

Client Solution
Client project

namespace WcfServiceConsole
Service Interface
public interface myInterface
Endpoint
A

Proxy
C

B

B

C

Endpoint
A

A

B

C

Service Implementation
public class myService :
myInterface
Main class
class Program {
static void Main(string[] args)
Y. Chen

24

Developing Services with Self-Hosting
Reading: Text Section 7.1

1. Start Visual Studio as an administrator:
right-clicking the program in the Start menu and
selecting “Run as administrator”; It is necessary as
the service needs to register an HTTP channel for
client to access;
2. Create a new Solution, with a new Project of
Console Application type and name
Service Implementation
the service “myService”;
public class myService :
myInterface
3. Create Contract Through Interface;
4. Implement the service behavior in C#
5. Add Hosting Service and Endpoint
Y. Chen

25

Starting a New Solution of Console Application Type
We use console application to create a service

Console application is
a basic template
without advanced tools

Y. Chen

26

Service Code in the Console Application:
Interface and Implementation
using System;
Service Contract
using System.ServiceModel;
is based on
using System.ServiceModel.Description;
interface
namespace ServiceConsole {
definition
[ServiceContract]
public interface myInterface { // Use interface to define contracts
[OperationContract] double PiValue();
Operation Contract
[OperationContract] int absValue(int intVal);
is based on method
}
public class myService : myInterface // implementation of interface Implementation
{ public double PiValue() {
interface
double pi = System.Math.PI; return (pi);
}
Service
public int absValue(int x) {
implementation
if (x >= 0) return (x); else return (-x);
(behavior)
}
} // not yet complete, to add hosting code next
Y. Chen

Project Overview

27

Service Solution: SelfhostingService

Client Solution
Client project

namespace WcfServiceConsole
Service Interface
public interface myInterface
Endpoint
A

Proxy
C

B

B

C

Endpoint
A

A

B

C

Service Implementation
public class myService :
myInterface
Main class
class Program {
static void Main(string[] args)
Y. Chen

28

Create Hosting Service and Endpoint
In the same program, add a class with the Main method;
1. Using Uri library class to create a URI instance to
myService as the base address;
Uri baseAddress = new
Uri("http://localhost:8000/Service");
2. Create a new ServiceHost
interface
instance to host the service Address
Service
implementation
ServiceHost selfHost = new
(behavior)
ServiceHost(typeof(myService),
baseAddress);
Service name

Base address included

Self-Hosting
Service

Y. Chen

Create Hosting Service and Endpoint

29

3. Add contract (myInterface) and Binding (WsHttpBinding)
Contract

selfHost.AddServiceEndpoint(typeof(myInterface),
new WsHttpBinding(),
"myService");
binding
interface
behavior
Endpoint

Address
Binding
Contract

Service
implementation
(behavior)

Self-Hosting
Service

Y. Chen

30

4.

Adding HTTP GET for Public Access
Add metadata for public access
System.ServiceModel.Description.ServiceMetadataBehavior
smb = new System.ServiceModel.Description.ServiceMetadataBehavior( );

smb.HttpGetEnabled = true;
selfHost.Description.Behaviors.Add(smb);

A different class in the namespace
System.ServiceModel.Description
will result in a different interface for
a different kind of clients to access.
As both SOAP-WSDL services and
RESTful services use HTTP GET,
the interface can potentially support
both types of services.

ServiceMetadataBehavior
(HTTP GET)
interface
Address
Binding
Contract

Service
implementation
(behavior)

Endpoint
Self-Hosting
Service

Y. Chen

31

Public Properties of ServiceMetadataBehavior Class

Property Name

Description

ExternalMetadataLocation Gets or sets a value that is the location of service
metadata
HttpGetEnabled

Gets or sets a value that indicates whether to publish
service metadata for retrieval using an HTTP/GET
request.

HttpGetUrl

Gets or sets the location of metadata publication for
HTTP/GET requests.

HttpsGetEnabled

Gets or sets a value that indicates whether to publish
service metadata for retrieval using an HTTPS/GET
request.

HttpsGetUrl

Gets or sets the location of metadata publication for
HTTPS/GET requests.

MetadataExporter

Gets or sets the internal MetadataExporter object used to
publish the service metadata.
Y. Chen

32

Starting and Terminating the Service
5. Start service and waiting for request.
selfHost.Open();
6. Close the ServiceHostBase to shutdown the service.
selfHost.Close();
We can configure
ServiceMetadataBehavior
to use WSDL or not

ServiceMetadataBehavior
(WSDL)
interface
Address
Binding
Contract

Service
implementation
(behavior)

.Open
.Close

Endpoint
Self-Hosting
Service
Y. Chen

33

Code Together: Hosting Part

class Program { // more detail see text section 7.1
static void Main(string[] args) {
// (1) Create a URI instance to myService as the base address.
Uri baseAddress = new Uri("http://localhost:8000/Service");
// (2) Create a new ServiceHost instance to host the service
ServiceHost selfHost = new ServiceHost
(typeof(myService),
baseAddress);
interface
try {
Address
// (3) Add contract
Service
Binding
implementation
selfHost.AddServiceEndpoint(
(behavior)
typeof(myInterface),
Contract
new WSHttpBinding(),
"myService");
Self-Hosting
Service

Y. Chen

34

Service Code: Hosting Part

// (4) Add metadata for public access.
System.ServiceModel.Description.ServiceMetadataBehavior smb =
new System.ServiceModel.Description.ServiceMetadataBehavior( );
smb.HttpGetEnabled = true;
selfHost.Description.Behaviors.Add(smb);
// (5) Start the service and waiting for request.
Without (4), the service will be
.Net - .Net remoting!
selfHost.Open();
Console.WriteLine("myService developed is ready to take requests. Please
create a client to call my double PiValue() service or int absValue(int)
service.");
Console.WriteLine("If you want to quit this service, simply press
<ENTER>.\n");
Console.ReadLine();
// (6) Close the ServiceHostBase to shutdown the service.
selfHost.Close();
}}}}
Y. Chen

Starting the Service

35





The service is fully developed and we can start the
service in Visual Studio by executing the command
“Start without Debugging”.

You could also add and edit the endpoint by editing
the Web.config file:
http://msdn.microsoft.com/en-us/library/ms734786(v=vs.110).aspx
Y. Chen

36

Project Overview: Service and Client
Service Solution:
namespace WcfServiceConsole
Console
Application

Service Interface
public interface myInterface
Endpoint
A

B

C

Endpoint

Proxy
C

B

A

B

C

A

Service Implementation
public class myService :
myInterface
Main class
class Program {
static void Main(string[] args)
Y. Chen

Client to Consume Self-Hosting Service
in a Console Application

37

Add a new Console Application in the same solution where the
self-hosting project resides;
 Right-click to add Reference: System.ServiceModel under
Assemblies  framework (It may have been added already in the
newer version of VS). Another way is to find it with the NuGet
package System.ServiceModel.Primitives
 Generate proxy to the service;
 The service is not hosted in IIS nor in IIS Express server;
 If WSDL is not generated in API, the proxy cannot be
generated by using “Add Service Reference” in your
programming environment.
Proxy
 Follow the next page to access the service:
C B A


Y. Chen

Creating the Proxy to Self-Hosting Service

38

Generate the proxy based on the metadata published
by the service.
 Use the Service Model Metadata Utility Tool
(SvcUtil.exe) tool to generate the proxy.


Generating
Proxy

Client

generated
Proxy.cs
App.config

Independent tool
SvcUtil.exe

ServiceMetadataBehavior
(WSDL)
interface

A

Address

B

Binding

C

Contract

Service
implementation
(behavior)

.Open

.Close

Endpoint
Self-Hosting
Service
Y. Chen

39

Use SvcUtil.exe Tool to Generate the Proxy
https://docs.microsoft.com/en-us/dotnet/framework/tools/developer-command-prompt-for-vs

Y. Chen

Use SvcUtil.exe Tool to Generate the Proxy

40

https://docs.microsoft.com/en-us/dotnet/framework/tools/developer-command-prompt-for-vs

Use DOS commands “dir”, “cd ..” and “cd folder” to
navigate into the directory where you have your client
project created in the previous step.
 While keeping your Service/API running, type the
following command in the command window to execute:






svcutil.exe /language:cs /out:generatedProxy.cs /config:App.
config http://localhost:8000/Service

This command will generate two files: App.config and
generatedProxy.cs, and place them into the directory:
C:\Windows\System32 or Search for them.

Y. Chen

Use SvcUtil.exe Tool to Generate the Proxy

41

Copy/Move the two files generatedProxy.cs and
App.config into you client project directory;
 Add them into your client project: Right-click the
client project in Solution Explorer, select Add 
Existing Item. Browse to the location of
generatedProxy.cs to add.
 Repeat the step above to add the App.config
configuration file: By default, the Add Existing Item
dialog box filters out all files with a .config extension.
To see these files select All Files (*.*) in the file type.
 Also read: Developer Command Prompt for Visual Studio


https://msdn.microsoft.com/en-us/library/ms229859(v=vs.110).aspx
Y. Chen

42

Add the Two Generated Files into Client

New files
added to the
project

Y. Chen

App.config File

Editing these default values

43

<?xml version="1.0" encoding="utf-8"?>
<configuration>
<system.serviceModel>
<bindings>
<wsHttpBinding>
<binding name="WSHttpBinding_myInterface"
closeTimeout="00:01:00" openTimeout="00:01:00"
receiveTimeout="00:10:00" sendTimeout="00:01:00"
bypassProxyOnLocal="false" transactionFlow="false"
hostNameComparisonMode="StrongWildcard"
maxBufferPoolSize="524288"
maxReceivedMessageSize="65536"
messageEncoding="Text" textEncoding="utf-8"
useDefaultWebProxy="true"
allowCookies="false">
<readerQuotas maxDepth="32" maxStringContentLength="8192"
maxArrayLength="16384" maxBytesPerRead="4096"
maxNameTableCharCount="16384" />
<reliableSession ordered="true" inactivityTimeout="00:10:00“ enabled="false" />
Y. Chen

44

App.config File
<security mode="Message">
<transport clientCredentialType="Windows"
proxyCredentialType="None" realm="" />
<message clientCredentialType="Windows"
negotiateServiceCredential="true"
algorithmSuite="Default" establishSecurityContext="true" />
</security>
</binding>
</wsHttpBinding>
</bindings>
<client>
<endpoint address= "http://localhost:8000/Service/myService"
binding="wsHttpBinding" bindingConfiguration="WSHttpBinding_myInterface"
contract="myInterface" name="WSHttpBinding_myInterface">
<identity> <userPrincipalName value="YinongBYENG\yinong" /> </identity>
</endpoint>
</client>
</system.serviceModel>
</configuration>

Y. Chen

45

generatedProxy.cs File

Y. Chen

46

Client Program in Console Application

class Program {
static void Main(string[] args) {
//(1) Create a proxy to the self-hosting service.
myInterfaceClient myProxy = new myInterfaceClient();
// (2) Call the service operations through the proxy
double pi = myProxy.PiValue(); // call PiValue operation
Int32 test1 = 27; // Create test input 1
Int32 test2 = -132; // Create test input 2
Int32 result1 = myProxy.absValue(test1); // call operation
Int32 result2 = myProxy.absValue(test2); // call operation
Console.WriteLine("PI value = {0}", pi);
Console.WriteLine("Absolute values of {0} is {1} and of {2} is {3}",
test1, test2, result1, result2);
myProxy.Close(); // (3) Close the proxy & channel to the service
Console.WriteLine("/nPress <ENTER> to terminate the client./n");
}
Y. Chen

47

Test client and self-hosting service
1. Right click myService project and
choose: Set as StartUp Project;
2. Start the project without debugging;
3. Right click the project
WCFClientConsole and choose
Debug  Start new instance.
If the console window closes at the end,
you can add Console.ReadLine at the
end of the client program to prevent the
console to close.

1

3

Y. Chen

48

Use Localhost and ASP .Net GUI Client
to Consume Self-Hosting Services
The previous example showed to create the proxy manually,
so that you have full control of the proxy. You can also
create the proxy automatically using Visual Studio tool.
 Step 1. Add an ASP .Net Web Application project into the
Self-Hosting Service Solution
 Right-click the Solution WcfServiceConsole. Select
Add  New  Web Application …
 Select ASP .Net Web App and the project “AspSite”
 Start the Service in localhost or in IIS
 Add Service Reference
 http://localhost:8000/Service/myService
Y. Chen

49

Add Service Reference

Y. Chen

50

C# Code Behind the ASPX Page
using System;
public partial class _Default : System.Web.UI.Page {
protected void Page_Load(object sender, EventArgs e) { }
protected void btnPiValue_Click(object sender, EventArgs e) {
AspProxyToWcf.myInterfaceClient refvar = new
AspProxyToWcf.myInterfaceClient();
double pi = refvar.PiValue();
lblPiValue.Text = Convert.ToString(pi);
}
protected void Button1_Click(object sender, EventArgs e) {
AspProxyToWcf.myInterfaceClient refvar = new
AspProxyToWcf.myInterfaceClient();
string abstxt = txtAbsoluteValue.Text;
int absVal = refvar.absValue(Convert.ToInt32(abstxt));
lblAbsoluteValue.Text = Convert.ToString(absVal);
}
}

Y. Chen

51

Use ASP .Net GUI Client
to Consume Self-Hosting Services

Y. Chen

Summary

52

Developing Basic Service Using WCF Template and
Server Hosting
 Developing API without Server Hosting:
Self-Hosting


Writing a service/API without using WCF tools
 Writing hosting service without using a server
 Creating Proxy without using “Add Service Reference”




Consuming WCF services
In Console Application
 In ASP .Net
 Many more: WF, BPEL, Cloud Computing, etc.


Y. Chen

